<x-layouts.app :domain-user="$domainUser" :administration-context="$administrationContext" title="Grootboekrekeningen"><div class="mx-auto max-w-5xl"><div class="mb-6 flex items-end justify-between gap-4"><div><p class="text-sm font-semibold text-blue-700">Beheer</p><h1 class="text-2xl font-bold">Grootboekrekeningen</h1><p class="mt-2 text-sm text-slate-600">Beheer rekeningmasterdata zonder saldo of openingsbalans.</p></div><a href="{{ route('settings.ledger-accounts.create') }}" class="min-h-11 rounded-lg bg-blue-700 px-4 py-3 font-semibold text-white">Rekening toevoegen</a></div>@error('master_data')<p class="mb-4 text-sm text-red-700" role="alert">{{ $message }}</p>@enderror<div class="overflow-x-auto rounded-xl border bg-white"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50"><tr><th class="p-3">Code</th><th class="p-3">Naam</th><th class="p-3">Type</th><th class="p-3">Status</th><th class="p-3">Acties</th></tr></thead><tbody>@forelse($accounts as $account)<tr class="border-t"><td class="p-3 font-semibold">{{ $account->code()->value() }}</td><td class="p-3">{{ $account->name()->value() }}</td><td class="p-3">{{ ucfirst($account->type()->value) }}</td><td class="p-3">{{ $account->isActive() ? 'Actief' : 'Inactief' }}</td><td class="p-3"><div class="flex flex-wrap gap-2"><a href="{{ route('settings.ledger-accounts.edit', $account->id()->toString()) }}" class="rounded border px-3 py-2">Wijzigen</a><form method="POST" action="{{ route($account->isActive() ? 'settings.ledger-accounts.deactivate' : 'settings.ledger-accounts.activate', $account->id()->toString()) }}">@csrf<button class="rounded border px-3 py-2">{{ $account->isActive() ? 'Deactiveren' : 'Activeren' }}</button></form></div></td></tr>@empty<tr><td colspan="5" class="p-5 text-slate-600">Nog geen grootboekrekeningen.</td></tr>@endforelse</tbody></table></div></div></x-layouts.app>

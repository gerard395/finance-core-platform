<x-layouts.app :domain-user="$domainUser" :administration-context="$administrationContext" title="Dagboeken">
    <div class="mx-auto max-w-5xl"><div class="mb-6 flex items-end justify-between gap-4"><div><p class="text-sm font-semibold text-blue-700">Beheer</p><h1 class="text-2xl font-bold">Dagboeken</h1><p class="mt-2 text-sm text-slate-600">Beheer dagboekmasterdata van de actieve administratie.</p></div><a href="{{ route('settings.journals.create') }}" class="min-h-11 rounded-lg bg-blue-700 px-4 py-3 font-semibold text-white">Dagboek toevoegen</a></div>
        @error('master_data')<p class="mb-4 text-sm text-red-700" role="alert">{{ $message }}</p>@enderror
        <div class="overflow-x-auto rounded-xl border border-slate-200 bg-white"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50"><tr><th class="p-3">Code</th><th class="p-3">Naam</th><th class="p-3">Type</th><th class="p-3">Status</th><th class="p-3">Acties</th></tr></thead><tbody>
        @forelse ($journals as $journal)<tr class="border-t border-slate-200"><td class="p-3 font-semibold">{{ $journal->code()->value() }}</td><td class="p-3">{{ $journal->name()->value() }}</td><td class="p-3">{{ ucfirst($journal->type()->value) }}</td><td class="p-3">{{ $journal->isActive() ? 'Actief' : 'Inactief' }}</td><td class="p-3"><div class="flex flex-wrap gap-2"><a href="{{ route('settings.journals.edit', $journal->id()->toString()) }}" class="rounded border px-3 py-2">Wijzigen</a><form method="POST" action="{{ route($journal->isActive() ? 'settings.journals.deactivate' : 'settings.journals.activate', $journal->id()->toString()) }}">@csrf<button class="rounded border px-3 py-2">{{ $journal->isActive() ? 'Deactiveren' : 'Activeren' }}</button></form></div></td></tr>@empty<tr><td colspan="5" class="p-5 text-slate-600">Nog geen dagboeken.</td></tr>@endforelse
        </tbody></table></div></div>
</x-layouts.app>

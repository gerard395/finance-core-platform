<x-layouts.app title="Inkoopfacturen" :$domainUser :$administrationContext>
    <div class="flex items-center justify-between"><div><h1 class="text-2xl font-bold">Inkoopfacturen</h1><p class="text-slate-600">Ontvangen leveranciersfacturen en openstaande verplichtingen.</p></div>@if($canManageDrafts)<a class="rounded-lg bg-blue-700 px-4 py-3 font-semibold text-white" href="{{ route('purchasing.invoices.create') }}">Nieuwe inkoopfactuur</a>@endif</div>
    <div class="mt-6 overflow-x-auto rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="bg-slate-50"><tr>@foreach(['Leverancier','Leveranciersfactuurnummer','Factuurdatum','Ontvangen','Vervaldatum','Bruto','Status'] as $h)<th class="px-4 py-3">{{ $h }}</th>@endforeach</tr></thead><tbody class="divide-y">@forelse($invoices as $item)<tr><td class="px-4 py-3">{{ $item->supplierName->toString() }}</td><td class="px-4 py-3"><a class="font-semibold text-blue-700" href="{{ route('purchasing.invoices.show',$item->id->toString()) }}">{{ $item->number->value() }}</a></td><td class="px-4 py-3">{{ $item->invoiceDate->format('d-m-Y') }}</td><td class="px-4 py-3">{{ $item->receivedDate->format('d-m-Y') }}</td><td class="px-4 py-3">{{ $item->dueDate->format('d-m-Y') }}</td><td class="px-4 py-3">{{ $moneyFormatter->format($item->gross) }}</td><td class="px-4 py-3">{{ ucfirst($item->status->value) }}</td></tr>@empty<tr><td colspan="7" class="p-8 text-center">Nog geen inkoopfacturen.</td></tr>@endforelse</tbody></table></div>
</x-layouts.app>

<x-layouts.app :domain-user="$domainUser" :administration-context="$administrationContext" title="Reconciliation corrigeren">
    <div class="max-w-3xl"><h1 class="text-2xl font-bold">Geïmporteerde bankboeking corrigeren</h1><p class="mt-2 text-slate-600">De immutable bankbron en oorspronkelijke financiële waarheid blijven historisch bestaan. Deze actie maakt via de bestaande B3-grens een contra-boeking.</p>
    <section class="mt-6 rounded-xl bg-white p-6 shadow-sm"><dl class="grid gap-3 sm:grid-cols-2"><div><dt class="text-sm text-slate-500">BankTransaction</dt><dd class="font-mono text-xs">{{ $source->financial->bankTransactionId->toString() }}</dd></div><div><dt class="text-sm text-slate-500">JournalEntry</dt><dd class="font-mono text-xs">{{ $source->financial->journalEntryId->toString() }}</dd></div><div><dt class="text-sm text-slate-500">Intentie</dt><dd>{{ $source->financial->intent->value }}</dd></div><div><dt class="text-sm text-slate-500">Originele PostingDate</dt><dd>{{ $source->financial->postingDate->value()->format('d-m-Y') }}</dd></div></dl></section>
    <form method="POST" action="{{ route('banking.reconciliation.reverse.store',$source->entry->id->toString()) }}" class="mt-6 grid gap-4 rounded-xl border border-red-200 bg-white p-6">@csrf<label>Correctie-PostingDate<input name="reversal_posting_date" type="date" required value="{{ old('reversal_posting_date',now()->format('Y-m-d')) }}" class="mt-1 w-full rounded-lg border-slate-300"></label><label>Reden<textarea name="reason" required maxlength="500" class="mt-1 w-full rounded-lg border-slate-300">{{ old('reason') }}</textarea></label><button class="rounded-lg bg-red-700 px-4 py-3 font-semibold text-white">Definitief terugdraaien</button></form></div>
</x-layouts.app>

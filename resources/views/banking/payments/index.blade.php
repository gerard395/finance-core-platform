<x-layouts.app title="Bankbetalingen" :$domainUser :$administrationContext>
<div class="flex items-center justify-between gap-4"><div><h1 class="text-2xl font-bold">Bankbetalingen</h1><p class="text-slate-600">Handmatige klantontvangsten en leveranciersbetalingen.</p></div>@if($canManage)<a class="rounded-lg bg-blue-700 px-4 py-3 font-semibold text-white" href="{{ route('banking.payments.create') }}">Nieuwe betaling</a>@endif</div>
<div class="mt-6 overflow-x-auto rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="bg-slate-50"><tr>@foreach(['Datum','Bankrekening','Type','Relatie','Referentie','Bedrag','Status','Boekingsdatum'] as $heading)<th class="px-4 py-3">{{ $heading }}</th>@endforeach</tr></thead><tbody class="divide-y">@forelse($payments as $detail)<tr><td class="px-4 py-3">{{ $detail->transaction->transactionDate()->value()->format('d-m-Y') }}</td><td class="px-4 py-3">{{ $detail->bankAccount->label()->value() }} · {{ $detail->bankAccount->iban()->value() }}</td><td class="px-4 py-3">{{ $detail->transaction->payment()->type()->value === 'customer_receipt' ? 'Klantontvangst' : 'Leveranciersbetaling' }}</td><td class="px-4 py-3">{{ $detail->relation->displayName()->toString() }}</td><td class="px-4 py-3"><a class="font-semibold text-blue-700" href="{{ route('banking.payments.show',$detail->transaction->id()->toString()) }}">{{ $detail->transaction->reference()->value() }}</a></td><td class="px-4 py-3">{{ $moneyFormatter->format($detail->transaction->amount()) }}</td><td class="px-4 py-3">{{ ucfirst($detail->transaction->status()->value) }}</td><td class="px-4 py-3">{{ $detail->posting?->posting->postingDate->value()->format('d-m-Y') ?? '—' }}</td></tr>@empty<tr><td colspan="8" class="p-8 text-center">Nog geen bankbetalingen.</td></tr>@endforelse</tbody></table></div>
</x-layouts.app>

<x-layouts.app :domain-user="$domainUser" :administration-context="$administrationContext" title="Bankafschrift">
    <h1 class="text-2xl font-bold">Bankafschrift {{ $item->externalId ?? 'zonder extern ID' }}</h1><dl class="mt-4 grid gap-3 rounded-xl bg-white p-6 shadow-sm sm:grid-cols-3"><div><dt class="text-sm text-slate-500">Opening</dt><dd>{{ $item->openingBalance ? $moneyFormatter->format($item->openingBalance) : 'Ontbreekt' }}</dd></div><div><dt class="text-sm text-slate-500">Mutaties</dt><dd>{{ count($item->entries) }}</dd></div><div><dt class="text-sm text-slate-500">Closing</dt><dd>{{ $item->closingBalance ? $moneyFormatter->format($item->closingBalance) : 'Ontbreekt' }}</dd></div></dl>
    <div class="mt-6 overflow-x-auto rounded-xl bg-white shadow-sm"><table class="min-w-full text-left"><thead><tr class="border-b"><th class="p-4">Boekdatum</th><th class="p-4">Bedrag</th><th class="p-4">Tegenpartij</th><th class="p-4">Referentie</th></tr></thead><tbody>@foreach($item->entries as $entry)<tr class="border-b"><td class="p-4"><a class="font-semibold text-blue-700" href="{{ route('banking.reconciliation.show',$entry->id->toString()) }}">{{ $entry->bookingDate->format('d-m-Y') }}</a></td><td class="p-4">{{ $moneyFormatter->format($entry->amount) }}</td><td class="p-4">{{ $entry->counterpartyName ?? '—' }}</td><td class="p-4">{{ $entry->accountServicerReference ?? $entry->entryReference ?? '—' }}</td></tr>@endforeach</tbody></table></div>
</x-layouts.app>

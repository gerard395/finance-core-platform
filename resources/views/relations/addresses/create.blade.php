<x-layouts.app :$domainUser :$administrationContext :$canViewRelations title="Nieuw adres">
<nav aria-label="Kruimelpad" class="text-sm text-slate-600">@if($canViewRelations)<a href="{{ route('relations.show', $relation->id()->toString()) }}" class="font-medium text-blue-700 focus:ring-2 focus:ring-blue-700">{{ $relation->displayName()->toString() }}</a><span aria-hidden="true"> / </span>@endif<span aria-current="page">Nieuw adres</span></nav>
<section class="mt-4 max-w-2xl rounded-xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h1 class="text-2xl font-semibold">Nieuw adres</h1><form method="POST" action="{{ route('relations.addresses.store', $relation->id()->toString()) }}" class="mt-6 space-y-5">@csrf
<div><label for="type" class="block font-medium">Type</label><select id="type" name="type" required class="mt-1 min-h-11 w-full rounded-lg border border-slate-300 px-3 focus:ring-2 focus:ring-blue-700"><option value="">Kies een type</option>@foreach($addressTypes as $type)<option value="{{ $type->value }}" @selected(old('type') === $type->value)>{{ $typePresenter::label($type) }}</option>@endforeach</select>@error('type')<p class="mt-2 text-sm text-red-700" role="alert">{{ $message }}</p>@enderror</div>
@include('relations.addresses.fields')
<div class="flex flex-wrap gap-3"><button class="min-h-11 rounded-lg bg-blue-700 px-4 font-semibold text-white focus:ring-2 focus:ring-blue-700">Adres toevoegen</button><a href="{{ $canViewRelations ? route('relations.show', $relation->id()->toString()) : route('app') }}" class="inline-flex min-h-11 items-center px-4 font-semibold text-blue-700 focus:ring-2 focus:ring-blue-700">Annuleren</a></div></form></section>
</x-layouts.app>

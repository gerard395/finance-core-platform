<x-layouts.app :$domainUser :$administrationContext :$canViewRelations title="Bankrekening bewerken">
<nav aria-label="Kruimelpad" class="text-sm text-slate-600">@if($canViewRelations)<a href="{{ route('relations.show', $relation->id()->toString()) }}" class="font-medium text-blue-700 focus:ring-2 focus:ring-blue-700">{{ $relation->displayName()->toString() }}</a><span aria-hidden="true"> / </span>@endif<span aria-current="page">Bankrekening bewerken</span></nav>
<section class="mt-4 max-w-2xl rounded-xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h1 class="text-2xl font-semibold">Bankrekening bewerken</h1><dl class="mt-4 grid gap-3 sm:grid-cols-2"><div><dt class="font-medium text-slate-600">IBAN</dt><dd>{{ $bankAccount->iban->value() }}</dd></div><div><dt class="font-medium text-slate-600">BIC</dt><dd>{{ $bankAccount->bic?->value() ?? 'Niet opgegeven' }}</dd></div></dl><form method="POST" action="{{ route('relations.bank-accounts.update', [$relation->id()->toString(), $bankAccount->id->toString()]) }}" class="mt-6 space-y-5">@csrf @method('PUT')<div><label for="account_name" class="block font-medium">Rekeningnaam</label><input id="account_name" name="account_name" value="{{ old('account_name', $bankAccount->accountName->value()) }}" required maxlength="255" @if($errors->has('account_name')) aria-invalid="true" @endif class="mt-1 min-h-11 w-full rounded-lg border border-slate-300 px-3 focus:ring-2 focus:ring-blue-700">@error('account_name')<p class="mt-2 text-sm text-red-700" role="alert">{{ $message }}</p>@enderror</div><div class="flex flex-wrap gap-3"><button class="min-h-11 rounded-lg bg-blue-700 px-4 font-semibold text-white focus:ring-2 focus:ring-blue-700">Wijzigingen opslaan</button><a href="{{ $canViewRelations ? route('relations.bank-accounts.show', [$relation->id()->toString(), $bankAccount->id->toString()]) : route('app') }}" class="inline-flex min-h-11 items-center px-4 font-semibold text-blue-700 focus:ring-2 focus:ring-blue-700">Annuleren</a></div></form></section>
</x-layouts.app>
